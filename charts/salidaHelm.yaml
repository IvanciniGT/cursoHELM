---
# Source: mies/templates/secrets.yaml
kind: Secret
apiVersion: v1

metadata:
    name:  RELEASE-NAME-secret

data:
    user: "ZWxhc3RpYw=="
    password: "U0szbXF4cGZZNmIwQURY"
---
# Source: mies/templates/elasticsearch/master/configmap.yaml
kind: ConfigMap
apiVersion: v1

metadata:
    name:  RELEASE-NAME-elasticsearch-maestro-srv-configmap

data:   
    otra_propiedad: "con su valor"
    ES_JAVA_OPTS: "-Xms220m -Xmx220m"
---
# Source: mies/templates/elasticsearch/master/service.yaml
apiVersion: v1
kind: Service


metadata:
  labels: 
    label1: "value1"
    name: RELEASE-NAME-elasticsearch-maestro-srv

spec:
    type: ClusterIP
    ports:
        - name: externo
          port: 9200
          targetPort: 9200
        - name: interno
          port: 9300
          targetPort: 9300
    selector:
        app:
---
# Source: mies/templates/kibana/service.yaml
apiVersion: v1
kind: Service

metadata:
  labels: 
    mode: "Ivan"
    mode2: "Ivancito"
    app: RELEASE-NAME-kibana
  name: RELEASE-NAME-kibana-srv

spec:
    type: ClusterIP
    ports:
        - port: 5601
          targetPort: 5601
    selector:
        app:  RELEASE-NAME-kibana


# NOMBRE DEL SERVICIO:
---
# Source: mies/templates/kibana/deployment.yaml
apiVersion: apps/v1
kind: Deployment

metadata:
  labels: 
    mode: "Ivan"
    mode2: "Ivancito"
    app:  RELEASE-NAME-kibana
  name:  RELEASE-NAME-kibana

spec:
  replicas: 1
  selector:
    matchLabels:
      app:  RELEASE-NAME-kibana
      
  template:
    metadata:
      labels: 
        mode: "Ivan"
        mode2: "Ivancito"
        app:  RELEASE-NAME-kibana

    spec:
      containers:
      - image: docker.elastic.co/kibana/kibana:7.8.0
        imagePullPolicy: IfNotPresent
        name: kibana
        ports:
        - containerPort: 5601
        env:
        - name: ELASTICSEARCH_HOSTS
          value: "http://elastic-coordinador:9200"
        - name:  SERVER_NAME
          valueFrom:
            fieldRef:
                fieldPath: metadata.name
        - name:  SERVER_HOST
          valueFrom:
            fieldRef:
                fieldPath: metadata.name

        - name:  ELASTICSEARCH_USERNAME
          valueFrom:
            secretKeyRef:
                name: RELEASE-NAME-secret 
                key:  user
                
        - name:  ELASTICSEARCH_PASSWORD
          valueFrom:
            secretKeyRef:
                name: RELEASE-NAME-secret 
                key:  password
---
# Source: mies/templates/elasticsearch/master/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet

metadata:
  labels: 
    app: RELEASE-NAME-elasticsearch-coordinator
---
# Source: mies/templates/elasticsearch/master/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet

metadata:
  labels: 
    app: RELEASE-NAME-elasticsearch-data
---
# Source: mies/templates/elasticsearch/master/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet

metadata:
  labels: 
    label2: "value2"
    app: RELEASE-NAME-elasticsearch-ingest
---
# Source: mies/templates/elasticsearch/master/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet

metadata:
  labels: 
    label1: "value1"
    app: RELEASE-NAME-elasticsearch-master
